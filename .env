# Frontend Environment Variables
# No need to set API URL - we use Vite's proxy to forward /api requests to backend

# Backend runs on port 5001
# Frontend proxy forwards /api/* to http://localhost:5001/api/*

